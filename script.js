const menuButton=document.querySelector('[data-menu-button]');const mobileNav=document.querySelector('[data-mobile-nav]');if(menuButton&&mobileNav){menuButton.addEventListener('click',()=>{const open=mobileNav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));menuButton.setAttribute('aria-label',open?'Close navigation menu':'Open navigation menu')});mobileNav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{mobileNav.classList.remove('open');menuButton.setAttribute('aria-expanded','false');menuButton.setAttribute('aria-label','Open navigation menu')}))}const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();if('IntersectionObserver'in window){const els=document.querySelectorAll('.reveal');const obs=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');obs.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -40px 0px'});els.forEach(el=>obs.observe(el))}else document.querySelectorAll('.reveal').forEach(el=>el.classList.add('is-visible'));
// Keep the two marquee tracks exactly one track-width apart so the seam is never visible.
function syncMarquees(){document.querySelectorAll('.marquee-wrap').forEach(wrap=>{const tracks=[...wrap.querySelectorAll('.marquee-track')];if(tracks.length<2)return;const first=tracks[0];const width=first.scrollWidth;tracks.forEach(t=>t.style.setProperty('--track-width',`${width}px`));});}
window.addEventListener('load',syncMarquees);window.addEventListener('resize',()=>{clearTimeout(window.__marqueeResize);window.__marqueeResize=setTimeout(syncMarquees,180)});
const form=document.querySelector('[data-contact-form]');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent(`Project enquiry — ${data.get('service')}`);const body=encodeURIComponent(`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nService: ${data.get('service')}\n\nProject details:\n${data.get('message')}`);window.location.href=`mailto:faysal.talukder36@gmail.com?subject=${subject}&body=${body}`;const status=document.querySelector('[data-form-status]');if(status)status.textContent='Your email app should open with the enquiry prepared.'})}